name: CI

on:
  push:
  pull_request:

jobs:
  frontend:
    name: Frontend CI
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./frontend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: echo "Install dependencies"
      
      - name: Run linter
        run: echo "Run linter"
      
      - name: Check formatting
        run: echo "Check formatting"
      
      - name: Run tests
        run: echo "Run tests"
      
      - name: Build
        run: echo "Build"

  backend:
    name: Backend CI
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./backend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: echo "Install dependencies"
      
      - name: Run linter
        run: echo "Run linter"
      
      - name: Run tests
        run: echo "Run tests"
